1. The CQL files in this folder will be applied to each organization's keyspace. 
2. A schema should NOT be included in any DDL statements. 
3. The schema will be specified when the CQL scripts are executed (e.g. USE myCompanySchema).
4. Organization specific data/information will be maintained in a separate folder.